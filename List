#include <cmath>
#include <iostream>
#include "j.h"
using namespace std;

int main()
{
    List j;
    for(int i=0; i<20;i++)
    { int  a=rand();
       int b=rand();
        add_List(&j,a,b);
    }
    int a,b;
    int k=10;
    cout<<"add ";
    cin>>a>>b;
    add_element(&j,a,b,k);
    find_first(&j,a,b);
    find_last(&j,a,b);
    print(&j);
    return 0;
}
